# Cloned PostHog docs — MIT-licensed under /contents/, but we don't vendor it.
# Run `python -m posthog_context.ingest` to reproduce.
data/posthog.com/
data/index.json

# Eval outputs are regenerated by `python -m eval.run`; the chart is committed
# because the README embeds it.
eval/out/*
!eval/out/comparison.png

.venv/
__pycache__/
*.egg-info/
.DS_Store
